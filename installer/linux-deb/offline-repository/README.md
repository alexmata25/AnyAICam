# Offline repository placeholder

The later signed Ubuntu 24.04 amd64 repository will contain the exact OS
dependency closure and a hash-pinned CPU-only OCI image. It is outside this
base-package checkpoint.
